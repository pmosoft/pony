<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.jdbc.mybatisLogback.oracle.EmpDao">

	<select id="selectEmp" resultType="test.jdbc.mybatisLogback.oracle.EmpDto">
		
		SELECT 'aa' empno, 'bb' ename 
		FROM    TDACM00060
		
	</select>
	

  <select id="selectSqlite" resultType="test.jdbc.mybatisLogback.oracle.EmpDto">
    
    SELECT 'aa' empno, 'bb' ename 
    FROM    TDACM00060
    
  </select>


  <select id="selectMariadb" resultType="test.jdbc.mybatisLogback.oracle.EmpDto">
    SELECT 'aa' empno, 'bb' ename from TPUSH00010
  </select>
 

	
</mapper>
